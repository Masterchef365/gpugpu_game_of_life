#version 450
layout (local_size_x = 16) in;
layout(set = 0, binding = 0) buffer Data {
    float data[];
} buf;

void main() {
    buf.data[gl_GlobalInvocationID.x] *= buf.data[gl_GlobalInvocationID.x];
}
